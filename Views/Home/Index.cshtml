
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>


@model Carton
<p>
    @Model.Matriz[0,0] @Model.Matriz[0,1] @Model.Matriz[0,2] @Model.Matriz[0,3] @Model.Matriz[0,4] @Model.Matriz[0,5] @Model.Matriz[0,6] @Model.Matriz[0,7] @Model.Matriz[0,8]
</p>
<p>
    @Model.Matriz[1,0] @Model.Matriz[1,1] @Model.Matriz[1,2] @Model.Matriz[1,3] @Model.Matriz[1,4] @Model.Matriz[1,5] @Model.Matriz[1,6] @Model.Matriz[1,7] @Model.Matriz[1,8]
</p>
<p>
    @Model.Matriz[2,0] @Model.Matriz[2,1] @Model.Matriz[2,2] @Model.Matriz[2,3] @Model.Matriz[2,4] @Model.Matriz[2,5] @Model.Matriz[2,6] @Model.Matriz[2,7] @Model.Matriz[2,8]
</p>

@{
    for ( int f = 0; f < 3; f++ ) {
        for ( int c = 0; c < 9; c++ ) {
            if ( @Model.Matriz[f, c] == 0 ) { //si es cero, mostramos espacios
                <p> 00 </p>
                ;
            } else {
                <p> @Model.Matriz[f, c] </p>
                ;
            }
        }
    }

}



@{
    for ( int f = 0; f < 3; f++ ) {
        for ( int c = 0; c < 9; c++ ) {
            if ( @Model.Matriz[f, c] == 0 ) { //si es cero, mostramos espacios
                   <p> 00 </p> ;
             } else {
                   <p> @Model.Matriz[f, c] </p>;
             }
        }
    }

}